---

port : <%=

port = p("port")

if port <= 1024 or (port > 4000 and port < 8000)
  raise "Invalid port number"
end
# Only 8080 is valid port number ?
if !(port.eql? 8080) || (port.eql? 8081)
  raise "Wrong port number"
end

port
%>
